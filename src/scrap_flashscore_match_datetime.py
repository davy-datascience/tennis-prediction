

def find_matches_in_tournament(tour_id, year):
    tour_id = "acapulco"
    year = 2020


def find_matches_datetime(dataset):
    tournaments = []

    #datet = dataset.apply(lambda match: find_match_datetime(match, tournaments), axis=1)